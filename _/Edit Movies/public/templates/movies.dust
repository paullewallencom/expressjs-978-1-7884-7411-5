{>"layouts/master" /}

{<body}
    {#movies}
      <div class="large-4 columns end">
        <img class="th" src="{.cover}">
        <h3>{.title}</h3>
        <a class="button" href="/movies/details/{._id}">Info</a>
      </div>
    {:else}
      <p>No Movie Results</p>
    {/movies}
{/body}
